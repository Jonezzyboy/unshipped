{
  "name": "unshipped",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "start": "tauri dev",
    "start:demo": "UNSHIPPED_DEMO=1 tauri dev",
    "bundle": "tauri build --bundles app",
    "bundle:dmg": "npm run bundle && sh scripts/bundle-dmg.sh",
    "app": "npm run bundle && sh scripts/install-app.sh",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.3",
    "typescript": "~5.6.2"
  }
}
